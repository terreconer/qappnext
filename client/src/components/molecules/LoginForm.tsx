import React from "react";
import { Button } from '@/components/atoms';

export const LoginForm: React.FC = () => {
  return (
    <div>
      <div>Login Form</div>
      <Button />
    </div>
  );
};
